# 3DWebPortfolio Agent Guide

This Unity project is `3DWebPortfolio`.

The correct Obsidian vault is:

`D:\ObsidianVaults\Unity\WebPortfolio`

That vault is the source of truth for project rules, checkpoints, architecture notes, prompt history, and AI workflow.

Do not use or modify:

`D:\ObsidianVaults\C++\LLGP\LowLevel-Y3`

unless explicitly instructed.

Prefer Unity Editor scripts over direct `.unity` or `.prefab` YAML edits. Direct YAML edits are allowed only for tiny, obvious, reviewable changes.

Preserve Unity `.meta` files. Keep Web performance and GitHub Pages deployment constraints in mind.
